import DisplayProp from "../components/DisplayProp";
import DisplayValue from "../components//DisplayValue";

export default function (Vue) {
  Vue.component("DisplayValue",DisplayValue);
  Vue.component("DisplayProp", DisplayProp);
}